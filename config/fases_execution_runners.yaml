# Shared runner definitions — used by all pipelines.
# Phase → runner assignment lives in config/<pipeline>/fase_runners.yaml.

runners:
  - GithubActions:
    # max-parallel: 2
    max-parallel: 20
    labels: [ubuntu-latest]
  - ESP32-self-hosted:
    labels: [self-hosted, Linux, X64, self-hosted-tehelab, ESP32v1]
    max-parallel: 1
  - Local:
    max-parallel: 1
    labels: [local]
  - K8s-8gb:
    max-parallel: 4
    labels: [runner-8gb]
  - K8s-24gb:
    max-parallel: 2
    labels: [runner-24gb]
